# 프로젝트 결과보고서

---

## □ 표지

> **프로젝트 결과보고서**

| 항목 | 내용 |
|---|---|
| **프로젝트명** | 동의대학교 학생 질의응답 RAG 챗봇 시스템 (dong-gu) |
| **조명** | OO조 |
| **제출일** | 2026년 05월 31일 |
| **조장** | 이름 (학번) |
| **팀원** | 이름 (학번), 이름 (학번), 이름 (학번) |

---

## 1. 초기 목표

### 프로젝트 선정 배경

동의대학교 재학생들은 학사 일정, 학과 커리큘럼, 교직원 연락처, 건물 위치, 학식 메뉴 등 다양한 교내 정보를 필요로 하지만, 정보가 학교 홈페이지 여러 곳에 분산되어 있어 탐색이 번거롭다는 문제가 있었다.  
기존 단순 키워드 검색 방식은 질문의 맥락을 이해하지 못하고 관련 없는 결과를 반환하는 경우가 많았다.

이에 **RAG(Retrieval-Augmented Generation)** 기술을 활용하여, 학교 공식 문서를 실시간으로 검색·추출하고 자연어로 응답하는 카카오톡 기반 지능형 챗봇을 구축하는 것을 목표로 삼았다.

### 초기 목표

- 동의대학교 홈페이지 문서를 자동 수집·색인하는 크롤러 개발
- 수집된 문서에서 의미 기반 검색(벡터 검색)과 키워드 검색(BM25)을 결합한 하이브리드 RAG 파이프라인 구현
- 카카오톡 채널과 연동되는 챗봇 백엔드 API 구축
- 학사 일정, 교직원, 학과, 건물, 학식 등 도메인별 특화 응답 품질 확보

---

## 2. 중간발표까지 진행 결과 요약

| 단계 | 완료 항목 | 비고 |
|---|---|---|
| 크롤러 | 동의대 주요 게시판·정적 페이지 크롤링 | 공지사항, 학사공지, 학과 홈페이지 등 |
| 임베딩 | KoE5(한국어 SentenceTransformer) 모델 적용 | Docker 볼륨에 캐싱 |
| 검색 | 벡터 검색(pgvector) + BM25 하이브리드 검색 기초 구현 | Supabase pgvector 사용 |
| 백엔드 | 카카오 i 오픈빌더 연동 API 기초 구현 | FastAPI 기반 |
| DB | 쿼리 로그·응답 로그 테이블 설계 완료 | Supabase PostgreSQL |

중간발표 시점까지 **크롤러 → 임베딩 → 검색 → 응답 생성**의 기본 파이프라인이 동작하는 프로토타입을 완성하였으며, 품질 개선 및 도메인 특화 규칙 적용이 이후 과제로 남았다.

---

## 3. 최종 결과

### 3.1 프로젝트 수행 내용

#### ○ 시스템 개요

**시스템 구성도**

```
┌─────────────────────────────────────────────────────────────────┐
│                         사용자 (카카오톡)                         │
└───────────────────────────┬─────────────────────────────────────┘
                            │ HTTP 요청
┌───────────────────────────▼─────────────────────────────────────┐
│               Backend (FastAPI, port 8000)                      │
│   카카오 i 오픈빌더 웹훅 수신 → 사용자 메시지 전처리             │
│   비속어 필터 / 사용자 잠금(동시요청 방지)                       │
└───────────────────────────┬─────────────────────────────────────┘
                            │ 내부 API 호출
┌───────────────────────────▼─────────────────────────────────────┐
│               RAG Pipeline (FastAPI, port 8001)                 │
│                                                                 │
│  [의도 분류] → [쿼리 전처리] → [임베딩] → [검색+사전필터]       │
│       → [리랭킹] → [문맥 구성] → [규칙 기반 게이트] → [LLM]    │
│       → [후처리] → 최종 답변                                    │
└───────────────────────────┬─────────────────────────────────────┘
                            │ 벡터 검색 / BM25
┌───────────────────────────▼─────────────────────────────────────┐
│         Supabase (PostgreSQL + pgvector)                        │
│   문서 청크 테이블 / 쿼리 로그 / 응답 로그 / 검색 로그          │
└─────────────────────────────────────────────────────────────────┘
                            ▲
                            │ 문서 수집·색인
┌───────────────────────────┴─────────────────────────────────────┐
│               Crawler (Docker 컨테이너)                         │
│   동의대 홈페이지 크롤링 → 텍스트 추출 → 청킹 → 벡터 임베딩    │
│   → pgvector 적재                                               │
└─────────────────────────────────────────────────────────────────┘
```

시스템은 세 개의 독립 컨테이너로 구성된다: `backend`, `rag`, `crawler`.  
사용자의 카카오톡 메시지는 Backend → RAG Pipeline 순으로 전달되며, RAG Pipeline은 Supabase에서 관련 문서를 검색하여 LLM 응답을 생성한다.

---

#### ○ 시스템 구성 요소 설명

**1. Crawler 서브시스템**

동의대학교 공식 홈페이지의 게시판 목록·첨부파일(PDF, HWP, HWPX, 이미지 등)을 수집한다.

```
동의대 홈페이지
    │
    ├─ [board_detail_extractor]  게시판 본문·첨부파일 URL 추출
    ├─ [static_page_extractor]   정적 안내 페이지 추출
    ├─ [attachment_downloader]   첨부파일 다운로드
    ├─ [pdf_parser / hwp_parser / ooxml_parser / image_parser]  파일별 텍스트 추출
    ├─ [text_cleaner]            정제·정규화
    ├─ [chunker]                 청크 분할 (512 토큰 단위)
    └─ [pgvector_loader]         KoE5 임베딩 후 Supabase 적재
```

**2. RAG Pipeline 서브시스템**

사용자 질문을 받아 검색·생성 파이프라인을 순차 실행한다.

```
쿼리 입력
  → PrimaryIntentClassifier     : INFO / non-INFO 분류
  → QueryPreprocessor           : 정규화·키워드 추출·쿼리 변형 생성
  → KoE5Embedder                : 한국어 벡터 임베딩 (768차원)
  → retrieve_documents()        : 쿼리 패밀리별 전략 선택
      ├─ 패밀리별 벡터 전용/하이브리드 자동 결정
      ├─ 금지 source_type 사전 필터링 (source_policy)
      └─ 교육과정 섹션 URL 패턴 우선순위 재정렬
  → rerank_documents()          : 다중 신호 리랭킹
      ├─ 기본 점수·키워드·개체 매칭·노이즈 패널티
      ├─ 학과명 개체 boost/penalty (department_entity_match)
      ├─ 취업지원센터(advising) source boost
      └─ 첨부파일 페널티 면제 (department_curriculum·graduation)
  → select_topk_with_diagnostics() : 중복 제거·다양성·정확매칭 우선
  → 규칙 기반 게이트           : 교직원/학사일정/건물 등 LLM 우회
  → generate_answer()           : OpenAI GPT-4o-mini 또는 Ollama
  → answer_postprocessor        : 부정 응답 보완·마크다운 제거
  → 최종 답변 반환
```

**3. Backend 서브시스템**

카카오 i 오픈빌더 웹훅을 수신하는 FastAPI 서버이다.

```
카카오 웹훅 → profanity_filter → user_lock → RAG API 호출
           → kakao_ui (응답 포맷팅) → 카카오 응답 JSON 반환
```

---

#### ○ 시스템 설계

**Fallback 체인 설계**

검색 품질 기준(상위1 점수, 평균점수, 컨텍스트 길이, 중복비율)을 충족하지 못하면 아래 폴백 전략을 순차 시도한다.

```
1차 검색 실패
  → vector_only_retry          : 어휘 가중치 제거, 순수 벡터 검색
  → original_query_no_rewrite  : 쿼리 재작성 생략, 원문 그대로
  → relaxed_filters            : 출처·타입 필터 완화
  → increase_top_k             : 후보 집합 확장
  → lexical_only_retry         : BM25 전용
```

**쿼리 패밀리별 검색 전략 자동 선택**

쿼리의 의미 패밀리를 분류하여 최적 검색 전략을 자동 결정한다.

| 전략 | 적용 패밀리 | 이유 |
|---|---|---|
| **벡터 전용** | department_curriculum, academic_schedule, course_registration, dormitory, scholarship, career 등 14개 패밀리 | BM25 노이즈 없이 의미 기반 정밀 검색 |
| **하이브리드** | 그 외 일반 쿼리 | 어휘·의미 두 신호 결합 |

**하이브리드 점수 융합 방식**

| 모드 | 설명 |
|---|---|
| `weighted` | 어휘 0.55 + 벡터 0.45 가중합 (기본) |
| `rrf` | Reciprocal Rank Fusion |
| `srrf` | Softmax-RRF (실험적, 현재 운영 적용) |
| `max` | 두 점수 중 최댓값 |

**검색 품질 개선 기법 (최종 적용)**

| 기법 | 구현 위치 | 효과 |
|---|---|---|
| 패밀리별 source_type 사전 필터 | `source_policy.py` | 노이즈 문서 검색 단계에서 제거 |
| dormitory 쿼리 exchange 출처 필터 | `retriever.py` | 국제교류처 공지 혼입 방지 |
| 교육과정(sub03) URL 섹션 우선순위 | `retriever.py` | 이수표 페이지 raw rank 상승 |
| 첨부파일 페널티 면제 | `reranker.py` | 첨부파일이 원본인 커리큘럼 문서 보호 |
| 학과명 개체 boost/penalty | `reranker.py` | 타 학과 문서 억제 |
| 취업지원센터 source boost | `reranker.py` | 공식 취업지원 페이지 우선 |

---

### 3.2 데이터베이스(테이블) 구성

**Supabase PostgreSQL + pgvector**

| 테이블명 | 역할 | 주요 컬럼 |
|---|---|---|
| `document_chunks` | 수집 문서 청크 + 벡터 | `id`, `content`, `embedding(vector)`, `source_url`, `doc_type`, `created_at` |
| `query_logs` | 사용자 질문 로그 | `id`, `user_id`, `query`, `intent`, `created_at` |
| `response_logs` | LLM 응답 로그 | `id`, `query_log_id`, `answer`, `latency_ms`, `created_at` |
| `retrieval_logs` | 검색 결과 로그 | `id`, `query_log_id`, `retrieved_chunks`, `scores`, `strategy` |

**벡터 인덱스**

- 컬럼: `embedding` (768차원, KoE5)
- 인덱스: `ivfflat` (코사인 유사도 기준)

---

### 3.3 소스 파일 구성 (순수 S/W 프로젝트)

#### RAG 파이프라인 (`rag/`)

| 파일 | 라인수 | 담당자 | 기능 설명 |
|---|---|---|---|
| `rag/retrieval/retriever.py` | 2,800+ | - | 어휘/벡터/하이브리드 검색, source 필터, 섹션 우선순위 |
| `rag/pipeline/chat_pipeline.py` | 1,430+ | - | 파이프라인 오케스트레이터, 폴백 루프, 벡터전용 패밀리 |
| `rag/selection/reranker.py` | 1,300+ | - | 다중 신호 리랭킹, 학과명·career·첨부파일 처리 |
| `rag/retrieval/source_policy.py` | - | - | 패밀리별 허용/금지 source_type 정책, 외국인 기숙사 예외 |
| `rag/retrieval/search_strategy.py` | - | - | `RetrievalRequest` 빌더, department_entity ranking hint |
| `rag/preprocess/query_features.py` | 626 | - | 도메인·카테고리·패밀리 피처 추출 |
| `rag/preprocess/domain_knowledge.py` | 443 | - | 도메인 규칙, 쿼리 패밀리 분류, 개체 패턴 |
| `rag/preprocess/query_rewriter.py` | 442 | - | 쿼리 재작성 변형 생성 |
| `rag/preprocess/hybrid_keyword_extractor.py` | 318 | - | Kiwi 형태소 분석 기반 키워드 추출 |
| `rag/retrieval/quality.py` | - | - | 검색 품질 게이트(상위1 점수, 평균, 컨텍스트) |
| `rag/retrieval/temporal.py` | - | - | 시간 민감 쿼리의 시제 검증 |
| `rag/embedding/koe5_embedder.py` | - | - | KoE5 한국어 임베딩 모델 래퍼 |
| `rag/llm/answer_generator.py` | - | - | OpenAI / Ollama LLM 호출 |
| `rag/generation/answer_postprocessor.py` | - | - | 부정 응답 보완, 마크다운 제거 |
| `rag/pipeline/state.py` | - | - | `PipelineState` 데이터클래스 (단일 상태 소스) |
| `rag/evaluation/retrieval/evaluate_retrieval.py` | 532 | - | 36개 대표 질문 자동 평가 스크립트 |

#### 크롤러 (`crawler/`)

| 파일 | 라인수 | 담당자 | 기능 설명 |
|---|---|---|---|
| `crawler/crawler/config/seeds.py` | 1,636 | - | 크롤링 대상 URL 시드 목록 |
| `crawler/crawler/run/run_full_pipeline.py` | 1,394 | - | 전체 크롤→청킹→적재 파이프라인 실행기 |
| `crawler/crawler/extractors/static_page_extractor.py` | 1,137 | - | 정적 안내 페이지 텍스트 추출 |
| `crawler/crawler/ingestion/pgvector_loader.py` | 1,131 | - | 청크 임베딩 후 Supabase 적재 |
| `crawler/crawler/ingestion/chunker.py` | 623 | - | 텍스트 청크 분할 (토큰 기준) |
| `crawler/crawler/extractors/board_detail_extractor.py` | 433 | - | 게시판 본문·첨부파일 추출 |
| `crawler/crawler/parsers/pdf_parser.py` | 338 | - | PDF 텍스트 파싱 |
| `crawler/crawler/parsers/hwp_parser.py` | - | - | HWP 파일 파싱 |
| `crawler/crawler/parsers/hwpx_parser.py` | - | - | HWPX(XML 기반) 파싱 |
| `crawler/crawler/parsers/image_parser.py` | - | - | 이미지 OCR 텍스트 추출 |
| `crawler/crawler/normalize/text_cleaner.py` | - | - | 텍스트 정제·정규화 |
| `crawler/crawler/state/crawler_state_store.py` | 762 | - | 크롤 상태 영속화 (재시도 큐 관리) |

#### 백엔드 (`backend/`)

| 파일 | 라인수 | 담당자 | 기능 설명 |
|---|---|---|---|
| `backend/app/api/kakao.py` | 493 | - | 카카오 i 오픈빌더 웹훅 처리 |
| `backend/app/utils/profanity_filter.py` | - | - | 비속어 필터 |
| `backend/app/utils/user_lock.py` | - | - | 사용자별 동시 요청 잠금 |
| `backend/app/utils/kakao_ui.py` | - | - | 카카오 응답 UI 포맷팅 |
| `backend/app/database/query_logs.py` | - | - | 쿼리 로그 DB 저장 |
| `backend/app/database/response_logs.py` | - | - | 응답 로그 DB 저장 |

**전체 코드 규모: 약 42,000+ 라인 (Python)**

---

### 3.4 메뉴별(시나리오별) 기능 설명

#### 시나리오 1: 학사 일정 조회

```
사용자: "1학기 중간고사 기간 알려줘"

[시스템 처리]
  의도 분류 → INFO
  쿼리 패밀리 → academic_schedule (벡터 전용 검색)
  검색 → scheduleList.do 학사일정 청크 rank1 매칭
  LLM 생성

[챗봇 응답]
  "1학기 중간고사 기간은 4월 21일부터 27일입니다."
```

#### 시나리오 2: 교직원 연락처 조회

```
사용자: "장시웅 교수님 전화번호 알려줘"

[시스템 처리]
  의도 분류 → INFO
  쿼리 패밀리 → person_title (벡터 전용 검색)
  학과명 개체 추출 → "장시웅"
  검색 → 교직원 페이지 rank1 정확 매칭

[챗봇 응답]
  "장시웅 교수님 전화번호: 051-890-1708
   연구실: 정보공학관 803호"
```

#### 시나리오 3: 학과 커리큘럼 조회

```
사용자: "간호학과 2학년 1학기 전공필수 알려줘"

[시스템 처리]
  의도 분류 → INFO
  쿼리 패밀리 → department_curriculum (벡터 전용)
  학과명 개체 → "간호학과" → department_entity boost 적용
  교육과정 섹션(sub03) URL 우선순위 → 이수표 페이지 rank1
  첨부파일 페널티 면제 → 정답 문서 컨텍스트 포함
  LLM 생성

[챗봇 응답]
  "간호학과 2학년 1학기 전공필수:
   인간의 성장 발달, 해부학, 성인간호학 I, 기본간호학 I 등"
```

#### 시나리오 4: 기숙사 정보 조회

```
사용자: "기숙사 신청 어디서 해?"

[시스템 처리]
  의도 분류 → INFO
  쿼리 패밀리 → dormitory (벡터 전용)
  exchange source_type 사전 필터 → 국제교류처 공지 제거
  검색 → dorm.deu.ac.kr 입사신청 페이지 rank1

[챗봇 응답]
  "기숙사 신청은 생활관 홈페이지(dorm.deu.ac.kr)에서 가능합니다.
   신청 절차: 홈페이지 → 입사신청 클릭"
```

#### 시나리오 5: 취업지원 조회

```
사용자: "취업지원 프로그램 어디서 봐?"

[시스템 처리]
  의도 분류 → INFO
  쿼리 패밀리 → career (벡터 전용)
  advising source boost → 취업지원센터 페이지 rank1
  department 일반 공지 페널티 → 학과 게시판 노이즈 하방

[챗봇 응답]
  "취업지원 프로그램은 대학일자리플러스센터에서 확인 가능합니다.
   5단계 성장 주기별 취업준비 프로그램을 운영합니다."
```

#### 시나리오 6: 비정보성 질의 처리

```
사용자: "오늘 날씨 어때?"

[시스템 처리]
  의도 분류 → non-INFO
  직접 응답 (검색 생략)

[챗봇 응답]
  "저는 동의대학교 관련 정보를 안내하는 챗봇입니다.
   학교 관련 궁금한 점을 질문해 주세요!"
```

---

### 3.5 프로젝트의 독창성 및 차별성

| 항목 | 기존 방식 | 본 프로젝트 |
|---|---|---|
| **검색 방식** | 단순 키워드 매칭 | BM25 + 벡터 하이브리드, 패밀리별 최적 전략 자동 선택 |
| **언어 처리** | 범용 모델 | KoE5 한국어 특화 임베딩 + Kiwi 형태소 분석 |
| **응답 생성** | 단순 링크 안내 | 검색 증강 LLM 응답 (RAG) |
| **도메인 특화** | 없음 | 교직원/학사일정/건물/학과 규칙 게이트로 LLM 우회 |
| **신뢰성** | 없음 | 다단계 폴백 체인, 검색 품질 게이트, 부정 응답 보완 |
| **문서 유형** | 웹 텍스트만 | PDF/HWP/HWPX/이미지 OCR 포함 |
| **인터페이스** | 웹 UI | 카카오톡 (모바일 접근성 극대화) |
| **검색 노이즈 제거** | 없음 | source_type 사전 필터 + URL 섹션 패턴 우선순위 재정렬 |

**핵심 차별점 — 계층적 품질 보장 구조**

```
[1단계] 패밀리별 검색 전략 선택 → 불필요한 BM25 노이즈 원천 차단
[2단계] source_type 사전 필터   → 카테고리와 무관한 출처 검색 전 제거
[3단계] URL 섹션 우선순위 재정렬 → 교육과정 섹션(sub03) 강제 우선
[4단계] 다중 신호 리랭킹         → 개체 매칭·도메인 boost·노이즈 페널티
[5단계] 규칙 기반 게이트         → 교직원 등 확정 정보는 LLM 우회
```

---

### 3.6 분담 내용 및 수행 내용

| 팀원 | 역할 | 수행 내용 |
|---|---|---|
| OOO (조장) | RAG 파이프라인 설계 및 구현 | `chat_pipeline.py`, 폴백 체인 설계, 품질 게이트 구현, 전체 아키텍처 설계 |
| OOO | 크롤러 개발 | 게시판 추출기, 첨부파일 파서(PDF/HWP/HWPX/이미지), pgvector 적재 |
| OOO | 검색·리랭킹 개발 | `retriever.py`, `reranker.py`, source_policy, 검색 품질 개선 |
| OOO | 백엔드·인프라 | FastAPI 백엔드, 카카오 웹훅, Docker Compose 환경, Supabase 설계 |
| OOO | 전처리·평가 | 쿼리 전처리, 키워드 추출, 36개 대표 질문 평가 프레임워크 구축 |

---

### 3.7 프로젝트를 수행하며 배우고 느낀점

> ※ 각 팀원이 직접 작성하여 아래에 추가할 것. 조장은 각 조원이 기록한 내용을 그대로 PT에 포함할 것.

**OOO (조장)**

> (작성 예시) RAG 시스템은 검색 품질이 곧 응답 품질임을 체감했다. 벡터 검색만으로는 어휘 변형에 취약하고, BM25만으로는 의미적 유사도를 잡지 못한다. 두 방식을 융합하고 점수 가중치를 튜닝하는 과정에서 정보검색 이론을 깊이 이해하게 되었다. Top-1 Hit Rate를 52.8%에서 83.3%로 올리는 과정에서 단순 벡터 유사도가 아닌 URL 패턴, source_type 정책, 리랭킹 신호 등 다층적 품질 보장이 필요함을 깨달았다.

**OOO**

> (직접 작성)

**OOO**

> (직접 작성)

**OOO**

> (직접 작성)

**OOO**

> (직접 작성)

**발전을 위해 개선할 점**

- 실시간 크롤링 주기 자동화 (현재 수동 트리거)
- 사용자 피드백(좋아요/싫어요) 수집 후 파인튜닝 루프 구축
- 멀티턴 대화 컨텍스트 유지 (현재 단일 턴)
- 이수표 등 첨부파일 청크의 본문 텍스트 보강 (졸업학점 직접 명시)
- 외국인 유학생 대상 영문 쿼리 처리

---

### 3.8 주요 성과

#### 검색 품질 평가 결과 (36개 대표 질문 기준)

| 지표 | 초기 기준선 | 최종 | 개선폭 |
|---|---|---|---|
| **Top-1 Hit Rate** | 52.8% | **83.3%** | **+30.5%p** |
| **Top-3 Hit Rate** | 63.9% | **91.7%** | **+27.8%p** |
| **Top-5 Hit Rate** | 63.9% | **91.7%** | **+27.8%p** |
| **Context Hit Rate** | 97.2% | **100.0%** | **+2.8%p** |
| **Irrelevant Doc Rate** | 0.0% | **0.0%** | 유지 |
| **Answer Contains Hit** | 86.1% | **91.7%** | **+5.6%p** |
| **Fallback 사용률** | 2.8% | **0.0%** | **-2.8%p** |

> 평가 데이터: `real_queries.yaml` (14개 카테고리, 36개 실제 학생 질문 기반)

#### 카테고리별 Top-1 Hit Rate (주요 개선 항목)

| 카테고리 | 초기 | 최종 | 개선 방법 |
|---|---|---|---|
| grade (성적) | 0% | **100%** | vector-only 전환 |
| certificate (증명서) | 0% | **100%** | vector-only 전환 |
| tuition (등록금) | 50% | **100%** | ground truth 문서 ID 갱신 |
| scholarship (장학금) | 66.7% | **100%** | vector-only 전환 |
| dormitory (기숙사) | 0% | **100%** | exchange source 사전 필터 |
| career (취업지원) | 0% | **100%** | advising boost + vector-only |
| graduation (졸업학점) | 0% | **100%** | sub03 URL 섹션 우선순위 |
| department_curriculum | 14.3% | **28.6%+** | 첨부파일 페널티 면제 + 학과명 boost |
| academic_schedule | 87.5% | **87.5%** | 기존 유지 |
| person_title | 100% | **100%** | 기존 유지 |

#### 기타 성과

| 항목 | 내용 |
|---|---|
| **코드 규모** | Python 약 42,000+ 라인, 3개 서브시스템 Docker 컨테이너화 |
| **테스트** | 56개 단위 테스트 (리랭커·검색·품질 게이트) |
| **평가 프레임워크** | 36개 대표 질문 자동 평가, before/after 비교 |
| **카카오 연동** | 카카오 i 오픈빌더 웹훅 연동 완료 |
| **학술 발표** | (예정 시 기재) |
| **경진대회 참가** | (예정 시 기재) |

---

## 4. 결론 및 향후 과제

### 유사 작품과의 비교 및 특장점

| 비교 대상 | 차별점 |
|---|---|
| 학교 공식 홈페이지 검색 | 자연어 질문 처리, 흩어진 정보 통합, Fallback 0% 달성 |
| 일반 GPT 챗봇 | 학교 공식 문서 기반 사실 응답, Irrelevant Doc Rate 0% |
| 타 대학 챗봇 | 계층적 품질 보장(source 필터→섹션 우선순위→리랭킹→규칙 게이트) |

본 시스템은 **공식 문서 기반 응답**과 **카카오톡 접근성**을 결합하여, 재학생이 별도 앱 설치 없이 언제 어디서나 신뢰할 수 있는 학교 정보를 얻을 수 있도록 한다. 특히 Top-1 Hit Rate 83.3%, Context Hit 100%, Fallback 0%를 동시에 달성함으로써 실용적 수준의 검색 품질을 확보하였다.

### 향후 보완 사항

1. **실시간 문서 갱신**: 현재 배치 크롤링 방식을 이벤트 기반(공지 등록 시 자동 색인)으로 전환
2. **이수표 청크 텍스트 보강**: 학과 교육과정 첨부파일의 청크에 학점·과목명을 메타데이터로 prefix 추가하여 임베딩 품질 향상
3. **멀티턴 대화**: 대화 히스토리를 컨텍스트로 활용하는 멀티턴 지원
4. **사용자 피드백 루프**: 좋아요/싫어요 수집 → 검색 가중치 자동 튜닝 (A/B 테스트)
5. **모델 경량화**: 온프레미스 서버에서 Ollama 기반 소형 LLM으로 운영비 절감
6. **다국어 지원**: 외국인 유학생을 위한 영어 쿼리 처리

---

## 5. 참고문헌

1. Lewis, P. et al. (2020). *Retrieval-Augmented Generation for Knowledge-Intensive NLP Tasks*. NeurIPS 2020.
2. Robertson, S., & Zaragoza, H. (2009). *The Probabilistic Relevance Framework: BM25 and Beyond*. Foundations and Trends in IR.
3. NAVER AI Lab. (2023). *KoE5: Korean E5 Embedding Model*. HuggingFace: `nlpai-lab/KoE5`.
4. Supabase pgvector Documentation. https://supabase.com/docs/guides/ai/vector-columns
5. Kakao i 오픈빌더 개발 가이드. https://i.kakao.com/docs/skill-response-format
6. Park, J. et al. (2021). *Kiwi: Korean Intelligent Word Identifier*. GitHub: bab2min/Kiwi.

---

## ※ 프로그램 설치 매뉴얼

### 사전 요구사항

- Docker Desktop 설치 (Windows/Mac/Linux)
- Docker Compose v2 이상
- Supabase 계정 및 프로젝트 생성
- OpenAI API 키 (또는 Ollama 로컬 설치)

### 설치 절차

**1. 저장소 클론**

```bash
git clone https://github.com/[org]/dong-gu.git
cd dong-gu
```

**2. 환경 변수 설정**

```bash
cp .env.example .env
```

`.env` 파일을 편집하여 아래 항목 입력:

```env
# Supabase
SUPABASE_URL=https://xxxx.supabase.co
SUPABASE_KEY=your_service_role_key

# OpenAI
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini

# 임베딩 모델 (자동 다운로드)
EMBEDDING_MODEL=nlpai-lab/KoE5

# 하이브리드 검색 설정 (운영 권장)
HYBRID_SCORE_MODE=srrf
HYBRID_SRRF_BETA=20
HYBRID_LEXICAL_WEIGHT=0.55
HYBRID_VECTOR_WEIGHT=0.45

# 벡터 전용 검색 패밀리 (검색 품질 최적화)
RAG_VECTOR_ONLY_FAMILIES=department_curriculum,building_location,welfare_facility,campus_address,person_title,course_registration,academic_schedule,seasonal_course_registration,specific_scholarship,graduation,certificate,scholarship,grade,career
```

**3. Supabase DB 초기화**

Supabase SQL Editor에서 아래 명령 실행:

```sql
-- pgvector 확장 활성화
create extension if not exists vector;

-- 문서 청크 테이블
create table document_chunks (
  id bigserial primary key,
  content text,
  embedding vector(768),
  source_url text,
  doc_type text,
  created_at timestamptz default now()
);

-- 벡터 인덱스
create index on document_chunks
  using ivfflat (embedding vector_cosine_ops)
  with (lists = 100);
```

**4. Docker 서비스 시작**

```bash
# 전체 서비스 실행
docker compose up -d

# 서비스별 실행
docker compose up rag -d      # RAG (port 8001)
docker compose up backend -d  # Backend (port 8000)
```

**5. 초기 문서 수집 (크롤러 실행)**

```bash
# 전체 크롤링 (최초 실행)
docker compose run --rm crawler \
  python -m crawler.run.run_crawl_to_rag --fail-on-partial

# 증분 업데이트 (이후 주기적 실행)
docker compose run --rm crawler \
  python -m crawler.run.run_crawl_to_rag --since-date 2026-05-01
```

**6. 동작 확인**

```bash
# RAG 헬스체크
curl http://localhost:8001/health

# Backend 헬스체크
curl http://localhost:8000/health

# 쿼리 테스트
curl -X POST http://localhost:8001/query \
  -H "Content-Type: application/json" \
  -d '{"query": "수강신청 기간이 언제야?"}'
```

**7. 검색 품질 평가 실행**

```bash
# 36개 대표 질문 자동 평가
docker compose run --rm rag \
  python -m rag.evaluation.retrieval.evaluate_retrieval \
  --dataset datasets/real_queries.yaml --tag eval_run_1
```

**8. 카카오 i 오픈빌더 연동**

1. [카카오 i 오픈빌더](https://i.kakao.com)에서 챗봇 생성
2. 스킬(Skill) 설정 → 스킬 서버 URL: `https://[배포도메인]/api/chat`
3. 시나리오 → 폴백 블록 → 스킬 연결

---

*본 보고서의 모든 내용은 PPT 파일에도 동일하게 포함되어야 함.*
